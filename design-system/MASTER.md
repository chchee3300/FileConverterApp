# MASTER — Estella Converter design-system record

> Read this top-to-bottom in under a few minutes to resume the migration cold. Full narrative plan lives at `usage-skills-implenment-happy-shamir.md` and its addendum `taste-skills-https-github-com-emilkowal-wondrous-shannon.md` in `~/.claude/plans/` — this file is the fast-reference companion, not a replacement.

## Do-not-touch list (check this before editing `resources/js/main.js`)

- **ffmpeg/qpdf command-building** — moved by Phase 0.4's extraction (was `resources/js/main.js:23, 1151, 1190, 1227, 1246, 1254` before extraction, now stale — corrected here 2026-07-14 to avoid exactly the stale-line-number trap this list exists to prevent). Now lives in `resources/js/lib/ffmpeg-commands.js` (`buildVideoCommand`, `buildImageCommand`, `buildAudioCommand`) and `resources/js/lib/qpdf-commands.js` (`buildPdfCommand`), called from `main.js:1069-1094`. Byte-identical behavior must survive every phase — verified via `test_conversion.js` before/after every change since.
- **State-class call sites**: `classList.add/remove/toggle` on `hidden|busy|error|dragover|drag-active` — **52 call sites** in `main.js` (verified via grep 2026-07-14; the original plan draft said 46 — that number was stale, this file is now the source of truth).
- **`getElementById` surface**: **128 call sites** in `main.js` (verified 2026-07-14) — every one of these IDs must exist in whatever markup replaces `index.html` in Phase 2, or be routed through the strangler-fig `lib/*.js` modules' inputs/outputs instead.
- `resources/js/neutralino.js` — Neutralino client, do not touch, do not port.

## Frozen token snapshot (dated 2026-07-14, from `resources/styles.css:8-71`)

Dark (`:root`, default):
| token | value |
|---|---|
| `--bg` | `#070809` |
| `--surface-0..3` | `#0c0e12` / `#111418` / `#171b22` / `#1e2330` |
| `--glass-bg` | `rgba(13,16,22,0.65)` |
| `--glass-border` / `--glass-border-hi` | `rgba(255,255,255,0.065)` / `rgba(255,255,255,0.12)` |
| `--glass-radius` | `10px` |
| `--text-primary/secondary/muted/dim` | `#eef0f4` / `#b8bcc8` / `#525a6e` / `#303544` |
| `--accent` / `--accent-dim` / `--accent-glow` | `#5b7fff` / `rgba(91,127,255,0.11)` / `rgba(91,127,255,0.22)` |
| `--danger` / `--success` | `#f04f4f` / `#3ecf8e` |
| `--sb-track/thumb/thumb-hi` | `rgba(255,255,255,.03/.07/.15)` |
| `--bg-grad` | radial-gradient, accent-tinted |
| `--header-h` / `--statusbar-h` | `2.75rem` / `1.625rem` |

Light (`[data-theme="light"]` override, same variable names): `--bg:#f2f3f7`, `--surface-0..3:#fff/#eef0f5/#e5e8ef/#d8dce8`, `--glass-bg:rgba(255,255,255,.75)`, `--text-primary:#0d1017`, `--accent:#4060e8`, `--danger:#d93535`, `--success:#1f9e6a` — full block at `resources/styles.css:43-71`.

Fonts: Inter + JetBrains Mono via Google Fonts `@import` (`styles.css:5`).

## ui-skills.com catalog snapshot (dated 2026-07-14, live-verified via `npx ui-skills`)

CLI: `npx ui-skills categories` / `list --category <cat>` / `get <slug>` (the last one just **prints markdown** — it does not install files; redirect output yourself). Raw category dumps saved at `design-system/ui-skills-recon-2026-07-14.txt`.

Confirmed skill slugs relevant to this project:
- `baseline-ui` — verified requirement text (`design-system` files aren't the source; live-fetched 2026-07-14): **MUST use Tailwind CSS defaults**, **MUST use accessible primitives (Base UI / React Aria / Radix)** for anything with keyboard/focus behavior, MUST use `motion/react` for JS animation. Confirms the plan's premise — gated to Phase 3 (post-migration).
- `ibelick/fixing-accessibility`, `ibelick/fixing-motion-performance`, `ibelick/fixing-metadata` — framework-agnostic, confirmed present in the `frontend` category listing.
- **New finding**: `leonxlnx/taste-skill` and `emilkowalski/emil-design-eng` are *also* directly in the ui-skills.com registry (categories `taste`/`visual`/`craft`), fetchable the same way as `baseline-ui` via `npx ui-skills get <slug>`. This means Phase 0.5 doesn't need a separate CLI (`npx skills add ...`) for these two — one mechanism covers both. The other 4 Emil Kowalski skills (`review-animations`, `improve-animations`, `animation-vocabulary`, `apple-design`) are **not** in ui-skills.com's registry — those still need the dedicated `github.com/emilkowalski/skills` installer.
- Also present in the `taste` category (not used by this plan, noted for awareness only): `leonxlnx/brutalist-skill`, `leonxlnx/gpt-tasteskill`, `leonxlnx/minimalist-skill`, `leonxlnx/soft-skill`, `leonxlnx/stitch-skill`, `pbakaus/{bolder,critique,delight,overdrive}`.

## Decisions log

- **2026-07-14** — Corrected stale fact: `main.js` has 52 state-class call sites, not 46 as the original plan draft stated. Recount before relying on any call-site number in future sessions; don't trust old counts without a fresh grep.
- **2026-07-14** — `taste-skill` and `emil-design-eng` installed via `npx ui-skills get <slug>` (ui-skills.com's own registry), not the separate `npx skills add` CLI — simpler, one fewer tool dependency.
- **2026-07-14** — `npx skills@latest add emilkowalski/skills` **failed**: the `skills` CLI (v1.5.17) imports `node:util`'s `styleText`, which requires Node ≥20.12; this machine runs Node v20.10.0. Worked around by shallow-cloning `github.com/emilkowalski/skills` and `github.com/muratcankoylan/Agent-Skills-for-Context-Engineering` into the scratchpad and copying only the needed skill folders into `.claude/skills/` (intact, not flattened), then deleting the clones. All 11 target skills confirmed present with intact `SKILL.md` under `.claude/skills/`: `taste-skill`, `emil-design-eng`, `animation-vocabulary`, `apple-design`, `improve-animations`, `review-animations`, `context-fundamentals`, `filesystem-context`, `context-compression`, `context-degradation`, `long-horizon-prompting`. If a future session tries `npx skills add` again, upgrade Node first or expect the same crash.
- **2026-07-14** — Extended golden-master suite added: `test_conversion.js` (new), 35/35 checks, drives the real app end-to-end (real ffmpeg/qpdf subprocess execution) across all 4 categories + filename-collision + live-estimate + progress-completion. `test_drop.js` (18/18) still passes unchanged. Both are now the Phase-gate regression net.
- **2026-07-14** — Found while writing `test_conversion.js`: every `<select class="input">` gets wrapped by `liquid-glass.js`'s `LiquidSelect` into a custom dropdown that sets the native `<select>` to `display:none` (`liquid-glass.js:253`). Naive Playwright `selectOption()`/`fill()` on these hangs or throws. Any future Playwright test touching format/bitrate/optimize dropdowns must set `.value` directly and dispatch `change`+`input` (bubbling) instead — mirrors what `LiquidSelect`'s own option-click handler does (`liquid-glass.js:308-315`). `type="range"` inputs have a similar Playwright `.fill()` reliability issue (unrelated to LiquidSelect) — same value+dispatch workaround used. See `setSelectValue`/`setRangeValue` helpers in `test_conversion.js`.
- **2026-07-14** — Observed gap, not fixed here (out of Phase 0 scope — a UI addition, not a rebrand task): `audio-format`'s `<select>` only offers MP3/WAV/AAC/FLAC (`index.html:186-191`); CLAUDE.md's mandatory regression checklist lists MP3/WAV/AAC/FLAC/**OGG** as the 5 formats to cover. OGG has no dropdown option today, so it was excluded from `test_conversion.js`'s audio coverage. Flag to the user — may be an intentional omission or a real gap.
- **2026-07-14** — Business logic extracted into `resources/js/lib/{filename-collision,ffmpeg-commands,qpdf-commands,progress-parser,size-estimate}.js` (classic `<script>` globals under `window.EstellaLib.*`, no bundler yet — matches the project's current zero-build-tooling setup; loaded in `index.html` before `main.js`). `main.js`'s execute handler and `updateEstimations()` now call these instead of inlining the logic. Both regression suites re-run **after** extraction: `test_conversion.js` 35/35, `test_drop.js` 18/18 — zero behavior change confirmed. These are the exact modules the future React port will import unchanged.
- **2026-07-14** — `taste-skill` scope check: its own Section 13 ("Out of Scope") says it is not for "dashboards / dense product UI / admin panels" and should say so explicitly rather than force-fit. This app is a single-window drag-drop conversion tool, not a landing/marketing page — so taste-skill's hero/CTA/brief-inference/block-library machinery (Sections 0, 2, 10, 12) does **not** apply here. Applied only its framework-agnostic parts: Section 8 (Dark Mode Protocol) and Appendix C (Liquid Glass honesty check).
- **2026-07-14** — Fixed, gated by both regression suites (35/35 + 18/18 unchanged after each): 
  - `resources/styles.css` `.lg-dropdown--closing` used `ease-in` (STANDARDS.md: "Never ease-in on UI — it starts slow, delaying the exact moment the user is watching"). Changed to the same strong ease-out curve already used for the matching open animation.
  - `prefers-reduced-motion: reduce` only covered `.glass-lens *` (AUDIT.md accessibility category: "movement with no prefers-reduced-motion handling"). Widened to `*, *::before, *::after` — the spin loader, file-item slideIn, statusbar pulse-dot, and lg-pop-down/up dropdown animations were previously unguarded.
  - `resources/js/liquid-glass.js`'s `LiquidSelect.openDropdown()` never set `transform-origin` on the portaled dropdown panel, so it always scaled from its own center regardless of which trigger it was anchored to (STANDARDS.md "Physicality": "origin-aware popovers... scale from the trigger, not center"). The function already computes an `openUpward` flip flag for positioning — reused it to set `transform-origin: top center` / `bottom center` to match.
  - Verified visually via Playwright screenshots in both dark and light theme, plus the dropdown open state — no layout regression, hierarchy parity holds in both themes (taste-skill 8.B).
- **2026-07-14** — Observed, deliberately **not** fixed this pass (logged so Phase 3's second-pass verification doesn't re-litigate these as new findings):
  - `#progress-bar` animates `width` (STANDARDS.md: only `transform`/`opacity` are GPU-cheap). Left as-is: it's a low-frequency, non-critical-path animation (one progress bar during file conversion, not a 60fps hot path), and switching to a `transform: scaleX()` structure would require restructuring the progress-bar markup and touches `test_conversion.js`'s assertions on `progressBar.style.width` — not worth the churn for a Phase 0 polish pass. Revisit if Phase 2's React port restructures this component anyway.
  - Several hand-typed `cubic-bezier(...)` curves in `styles.css` are close-but-not-identical (STANDARDS.md "Cohesion & tokens": consolidate into shared `--ease-*` tokens). Left alone — consolidating would touch many already-tuned, unrelated animations at once, higher risk than this pass's scope justifies.
  - Light theme's `--surface-0: #ffffff` is a pure white (taste-skill 8.B: "no pure #ffffff... pure values kill depth"). This guidance reads as landing-page-oriented; pure-white base surfaces are common and generally accepted in dense utility UIs. Not changed.
- **2026-07-14** — Phase 1 toolchain versions were **not** the latest available, deliberately, after checking `engines` fields against this machine's Node v20.10.0:
  - `vite@8.1.4` (latest) and `vite@7.x` require Node `^20.19.0 || >=22.12.0` — incompatible. `vite@5.x` (`^18.0.0 || >=20.0.0`) is compatible but ships `esbuild@0.21.x`, which has a known moderate dev-server-only CORS advisory (GHSA-67mh-4wv8-2f99) with no in-range fix. **`vite@^6.4.3`** turned out to be the sweet spot: engines `^18.0.0 || ^20.0.0 || >=22.0.0` (compatible) AND ships the patched `esbuild@^0.25.0` — `npm audit` is clean. Installed `vite@^6.4.3`.
  - `@vitejs/plugin-react@6.x` (latest) also requires Node `^20.19.0` — used `@vitejs/plugin-react@^4.7.0` instead (peer-compatible with `vite@^6`, engines `^14.18.0 || >=16.0.0`).
  - `react@19.2.7` / `react-dom@19.2.7` (latest) — no engine constraint, used as-is.
  - `tailwindcss@^4.3.2` / `@tailwindcss/vite@^4.3.2` (latest) — no engine constraint, no peer conflict with `vite@^6`.
  - If a future session upgrades Node past 20.19, revisit whether `vite@7`/`8` and `@vitejs/plugin-react@6` are worth adopting — no urgency, current setup has zero known vulnerabilities.
- **2026-07-14** — `vite.config.js` failed to load (`ERR_REQUIRE_ESM`): with no `"type": "module"` in `package.json` (deliberately not added — would break every existing CommonJS `test_*.js` script), Vite's deprecated CJS config loader chokes on `@tailwindcss/vite`'s ESM-only build. Fixed by naming the file **`vite.config.mjs`** instead, which forces Vite's native-ESM config loader regardless of `package.json`'s `"type"`. No `package.json` changes needed, all `test_*.js` scripts still run as CommonJS unaffected.
- **2026-07-14** — Original plan text said "port tokens into `tailwind.config.js` `theme.extend`" — written before Phase 0's live recon. Tailwind's current major (v4, what `npm view tailwindcss version` resolves to today) replaced that with CSS-native `@theme` blocks; a JS config file isn't the idiomatic v4 path. Same intent (utilities resolve to the app's real token values), different mechanism because v4 is now current. Ported into `src/index.css`'s `@theme` block: `--color-bg/surface-0..3/text-primary..dim/accent/danger/success/glass-bg/glass-border/glass-border-hi`, `--radius-glass`. `--header-h`/`--statusbar-h` kept as plain `:root` vars (one-off layout constants, not spacing-scale additions) for later arbitrary-value use (`h-[var(--header-h)]`). **Light theme override was deliberately not ported yet** — deferred to Phase 2 when the real theme toggle exists and Tailwind's dark-mode variant strategy can be wired against it; Phase 1's only job was proving the mechanism works, verified via a Playwright screenshot of the built output.
- **2026-07-14** — Fetched `baseline-ui`, `fixing-accessibility`, `fixing-motion-performance` into `.claude/skills/` (plan step 4) — cached now, applied in Phase 2/3 against real components, not yet. Also checked `fixing-metadata` (flagged "likely present, confirm in Phase 0" but never actually confirmed until now): it exists, but is almost entirely SEO/web-crawling focused (OG tags, canonical URLs, robots directives, JSON-LD) — none of which applies to a desktop Neutralino app with no public web presence. **Not** cached, same scope-honesty reasoning as the `taste-skill` check above. Revisit only if the app ever grows a public web/marketing surface.
- **2026-07-14** — Vite build output directory is **`web-dist/`**, not `dist/` — `dist/` is already Neutralino's own `neu build` packaging output (confirmed via `ls dist/FileConverterApp/`); reusing the name would collide. Added `/web-dist` to `.gitignore`. `neutralino.config.json`'s `documentRoot` is still `/resources/`, unchanged — confirmed via `git diff` showing zero changes to `neutralino.config.json`/`resources/` this phase, plus both regression suites (35/35, 18/18) re-run clean after all of Phase 1's changes.
- **2026-07-14 (Phase 2.1)** — **Scope decision for all of Phase 2**: component classes (`.header`, `.statusbar`, `.panel`, `.btn`, etc.) are reused verbatim by importing `resources/styles.css` directly into `src/index.css` (single source of truth, not duplicated), rather than re-expressing ~1478 lines of hand-tuned glassmorphism CSS as Tailwind utilities while also porting complex interactive logic (drag/drop, the trim scrubber) with no visual-regression safety net yet. Structural/behavioral fidelity is Phase 2's job; baseline-ui's Tailwind-ification pass is Phase 3's, once the golden-master suite can gate it directly against the React build (Phase 2.6). The Phase 1 `@theme` token block stays alongside it for incremental Tailwind-utility use where it makes sense.
- **2026-07-14 (Phase 2.1)** — Importing `../resources/styles.css` *after* `@import "tailwindcss"` produced a build warning ("@import rules must precede all rules aside from @charset and @layer statements") — Tailwind v4's import expands inline into `@layer` rules, so any CSS `@import` after it violates CSS's import-ordering rule even though both `@import` lines were source-adjacent. Fixed by putting the `resources/styles.css` import **first**, `tailwindcss` second. Clean build, no warnings, after the swap.
- **2026-07-14 (Phase 2.1)** — Header, StatusBar, and the theme toggle ported to `src/components/{Header,StatusBar}.jsx` + `src/hooks/useTheme.js`, matching `resources/index.html:23-37,247-252` and `main.js`'s theme IIFE (`main.js:521-536`) and `setStatus()` behavior exactly — same class names, same `id`s (kept for CSS-selector compatibility, e.g. `styles.css`'s `#theme-toggle { ... }`), same `localStorage` key (`estella-theme`), same `data-theme` attribute target. Root `index.html` got the same flash-prevention inline script as `resources/index.html:11-17`. Verified via Playwright screenshots in both themes — pixel-equivalent to the vanilla app's header/statusbar. Caught and fixed a bug pre-verification: an early draft called `useTheme()` twice in `App.jsx` (once for the side effect, once inline for the toggle handler), which would have created two desynced state instances after the first toggle — fixed to a single call, destructured.
- **2026-07-14 (Phase 2.2+2.3+2.4)** — **Scope re-merge, discovered mid-implementation**: the plan's slice 4 (ProgressBar) also turned out coupled to the same lifted state as slices 2/3 (the execute handler needs every settings field + files + fileType to build commands), so it was folded into this same push too, extending the user-approved 2+3 merge. Slice 5 (Trim modal) stays separate — self-contained enough not to block the main flow.
- **2026-07-14 (Phase 2.2+2.3+2.4)** — `<select class="input">` elements are wrapped in a `GlassSelect.jsx` component (`src/components/GlassSelect.jsx`) that calls `LiquidSelect.create()` on mount and `LiquidSelect.sync()` whenever React's `value` prop changes — mirrors what `LiquidSelect`'s own option-click handler already does internally (`liquid-glass.js:308-315`). The `disabled` attribute needs no special handling: `LiquidSelect.create()` already installs a `MutationObserver` watching it (`liquid-glass.js:449-450`), so React's `disabled={...}` prop just works.
- **2026-07-14 (Phase 2.2+2.3+2.4)** — Files/fileType/outputPath/settings lifted from `main.js`'s module-level `let` globals into React state (`useFileManager.js`, `useSettings.js`, `useExecute.js`). One real race condition found and fixed pre-verification: vanilla's `importDroppedFiles` relies on synchronously reading its own write to `outputPath` (sets it to Downloads, then immediately calls `handleFiles`, which checks `!outputPath` and must see the just-written value, not the temp dir — see the comment at that call site in `main.js`). A plain `useState` setter doesn't commit before the next line runs, so `useFileManager.js` mirrors state into a ref (`outputPathRef`) and writes both together (`setOutputPathBoth`) specifically where this same-flow read-after-write matters. No other state in this port needed the same treatment (checked each call site).
- **2026-07-14 (Phase 2.2+2.3+2.4)** — `updateEstimations()`'s per-file DOM update loop became unnecessary in React: `computeEstimate()` (`src/lib/estimateDisplay.js`) is a pure function of current file + settings, so `FileList.jsx` just calls it inline during render and gets the same result for free, with no imperative "call this after every relevant change" step. One vanilla UX quirk preserved on purpose, not fixed: `#image-resolution-preview` is a single settings-panel-level element that updateEstimations() overwrote on every loop iteration, so with multiple files it only ever reflected the *last* file in the list (`main.js:107-114` pre-extraction) — `ImageSettings` in `SettingsPanel.jsx` reproduces this by deriving from `files[files.length - 1]`, not "fixing" it to something more sensible. Also preserved verbatim: `FileList.jsx`'s inline styles use `var(--muted)`/`var(--accent)`, but only `--text-muted` is actually defined in `styles.css` — `--muted` is undefined, so those spans silently inherit color today. Pre-existing vanilla bug, not introduced by this port; flagging here rather than fixing it mid-port.
- **2026-07-14 (Phase 2.2+2.3+2.4)** — Two real, non-obvious integration bugs found only by testing against the **real** Neutralino native API (not just `npm run build` succeeding) — this is why the temporary documentRoot-switch verification mattered instead of deferring everything to Phase 2.6:
  1. `resources/js/neutralino.js` is a classic-script global (`var Neutralino = ...`, no explicit `window.Neutralino = ...`), unlike `liquid-glass.js` and the `lib/*.js` modules (which explicitly do `window.X = ...` and are therefore safe to bundle as plain ES side-effect imports). Originally tried loading it via a Vite `?raw` import injected into a runtime `<script>` element (`loadNeutralino.js`, now deleted) — this technically got `window.Neutralino` defined, but broke a *second*, non-obvious thing (see below), so it was replaced with a genuine `<script src="/js/neutralino.js">` tag in `index.html`, placed before the module script so it's guaranteed to execute first (classic scripts block; `type="module"` scripts are deferred).
  2. **`neu`'s dev server injects `window.NL_PORT`/`NL_TOKEN`/etc. into its HTTP response for the exact file configured as `neutralino.config.json`'s `cli.clientLibrary`** — matched by resolving `documentRoot` + the requested URL path against that configured value, not by rewriting the served HTML. Bundling `neutralino.js`'s source at build time (the `?raw` approach) never makes that HTTP request, so the injection hook never fires and `window.NL_PORT` stays `undefined` — the WebSocket connect then throws `Failed to construct 'WebSocket': ... 'ws://localhost:undefined?...' is invalid`. Confirmed by fetching the raw served HTML directly (`curl`) and finding zero injected script content. Fixed by switching to the real `<script src>` tag (point 1) so the browser's own request triggers the injection, exactly like `resources/index.html` already does.
  3. **Still open for Phase 2.6**: once `documentRoot` permanently becomes `/web-dist/`, `neutralino.js` needs to actually exist on disk at whatever path the `<script src>` in `index.html` requests (currently `/js/neutralino.js`, i.e. `web-dist/js/neutralino.js`), *and* `cli.clientLibrary` needs updating to match (currently `/resources/js/neutralino.js`, which only resolves correctly while `documentRoot` is `/resources/`). Verified this works by manually copying the file and temporarily editing both config values, then reverting — Phase 2.6 needs a **permanent, automated** version of that copy step (e.g. a small Vite plugin or `postbuild` script), not a manual one.
- **2026-07-14 (Phase 2.2+2.3+2.4)** — Testing note for Phase 2.6 (adapting `test_conversion.js` for the React build): React-controlled `<input type="range">`/text inputs need the **native property setter bypass** for programmatic value changes to be detected — `element.value = x` followed by `dispatchEvent(new Event('input'))` (the pattern `test_conversion.js`'s `setRangeValue` already uses successfully against the *vanilla* app) silently no-ops against a React-controlled input, because React overrides the `value` property setter to track "real" user-driven changes. Use `Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value').set.call(el, x)` before dispatching instead. Confirmed this is a test-tooling quirk only, not an app bug — genuine user drag interactions always fire real native events and are unaffected. `GlassSelect`'s plain `el.value = x; dispatchEvent(...)` pattern, by contrast, worked fine with no special handling needed.
- **2026-07-14 (Phase 2.5)** — `TrimModal.jsx` ported from `resources/index.html:253-312` + main.js's Trim Modal Logic (`main.js:538-867` pre-extraction) — the most complex interactive widget in the app: a custom dual-thumb drag slider plus a video/audio preview player mounted via `Neutralino.server.mount`. `formatTrimTime`/`parseTrimTime` (`main.js:541-559`) were dead code — defined, never called anywhere in `main.js` — not ported, no behavior to preserve. Design choice: `trimStart`/`trimEnd` (and everything derived from them — thumb positions, range/dim overlays, labels) are React state, driving the same inline-style positioning declaratively instead of an imperative `updateSliderUI()` DOM-write function; the playhead position (updated on every `timeupdate` during playback — much higher-frequency and longer-running than a drag gesture) stays a direct ref-based DOM write to avoid a re-render per frame, same performance profile as the vanilla direct style write.
- **2026-07-14 (Phase 2.5)** — Verified against the real Neutralino API again (same temporary documentRoot/clientLibrary switch + reverted, per the Phase 2.2+2.3+2.4 precedent) — 11/11 checks: modal open/close, real video preview mount, drag-driven trim range changes, save-then-reopen persistence, and clear+cancel correctly *not* persisting (matches vanilla: Clear only resets the modal's local state, only Save commits to the file). Two rounds of test-script bugs caught and fixed before trusting the results, not app bugs:
  1. Several `check(name)` calls were missing their condition argument (`check('T1: trim modal opens');` with no second parameter — same class of mistake as Phase 0's `test_conversion.js` draft), registering false failures for things that had actually succeeded (proven by later checks in the same run depending on them and passing).
  2. A dead-center simulated click on `#trim-thumb-right` landed on its child `.thumb-grip` (2px wide, no `pointer-events: none`) instead of the thumb itself, so `e.target` didn't match the container's `e.target === thumbLeft/thumbRight` identity check (`TrimModal.jsx`, mirroring `main.js:795` exactly) and the drag got reinterpreted as a playhead-seek instead. **This exact edge case exists identically in the vanilla app** (same CSS, same unguarded identity check) — confirmed by reading `main.js:794-801` side by side. Not a port regression; a pre-existing latent bug in both versions that a precise scripted click exposes far more reliably than an imprecise human one. Retested with an off-center click (avoiding the grip) and the drag worked correctly. Not fixed in either version — out of scope for a structural port; flagging here in case it's worth a real fix later (e.g. add `pointer-events: none` to `.thumb-grip`, or have the thumb's own `mousedown` call `e.stopPropagation()`).

## Phase checkpoints (mechanical success predicates)

- **Phase 0 done** iff: `design-system/MASTER.md` exists (this file) AND Playwright suite covers all 4 conversion categories + shared behaviors and passes AND `resources/js/lib/*.js` extraction exists with zero behavior change AND `.claude/skills/{taste-skill,emil-design-eng,review-animations,improve-animations,animation-vocabulary,apple-design,context-fundamentals,filesystem-context,context-compression,context-degradation,long-horizon-prompting}/SKILL.md` all exist AND the taste/animation pass on the current vanilla app is logged above AND `resources/` is still what `neutralino.config.json` serves.
- **Phase 1 done** — ✅ complete 2026-07-14: `vite.config.mjs` + `src/{main.jsx,App.jsx,index.css}` exist AND `npm run build` succeeds (outputs to `web-dist/`) AND `neutralino.config.json` still points at `resources/` (verified via `git diff`, zero changes) AND `src/index.css`'s `@theme` block contains the dark-mode color/radius tokens from the frozen snapshot (light-theme override intentionally deferred to Phase 2) AND both regression suites pass unchanged (35/35, 18/18) AND a Playwright screenshot of the built output visually confirms the tokens render correctly.
- **Phase 2 done** iff: all 5 component slices (shell, DropZone/FileList, SettingsPanel, ProgressBar, Modals/Trim) are ported AND the Phase 0 Playwright suite passes against the React build AND `neutralino.config.json` now serves the Vite build.
  - Slice 1/5 (shell: Header/StatusBar/theme toggle) — ✅ done 2026-07-14, `src/components/{Header,StatusBar}.jsx` + `src/hooks/useTheme.js`, verified via screenshot in both themes, production app confirmed untouched (both suites still 35/35 + 18/18).
  - Slices 2/3/4 (DropZone/FileList + SettingsPanel + ProgressBar/Execute) — ✅ done 2026-07-14, merged into one push (see decisions log: these 3 turned out coupled from day one, not independent as originally scoped). Verified against the **real Neutralino native API** (not just build success) via a temporary documentRoot switch + probe test, then reverted — 12/12 checks, including a real file-drop → real ffmpeg conversion → real output file. Production app (`resources/`, `neutralino.config.json`) confirmed untouched afterward via `git diff` + both suites still 35/35 + 18/18.
  - Slice 5 (Modals/Trim) — ✅ done 2026-07-14, `src/components/TrimModal.jsx`, verified against the real Neutralino API (11/11 checks: mount/drag/save/reload/cancel). Production app confirmed untouched afterward (`git diff` clean + both suites 35/35 + 18/18).
  - **All 5 slices done.** Remaining for Phase 2: the 3rd item in the checkpoint predicate above — `neutralino.config.json` permanently serving the Vite build + the Phase 0 Playwright suite passing directly against it (Phase 2.6, see the still-open `neutralino.js`-under-`web-dist` serving item in the Phase 2.2+2.3+2.4 decisions above).
- **Phase 3 done** iff: `baseline-ui`/`fixing-accessibility`/`fixing-motion-performance`/`fixing-metadata` second-pass checks logged above AND old `resources/index.html`/`styles.css`/vanilla `main.js` deleted AND this file's decisions log is closed out.

## Tooling surface (which skill produced which decision)

Installed project-locally under `.claude/skills/` (Phase 0.5). When a design/motion/a11y change lands, note which skill's audit produced it here vs. an ad hoc judgment call — keeps Phase 3's second-pass verification honest about what's already covered.

- `review-animations` (`STANDARDS.md`) + `improve-animations` (`AUDIT.md`) drove all 3 Phase 0.6 fixes above (ease-in, reduced-motion coverage, transform-origin).
- `taste-skill` drove the Section-13 scope check (this app isn't its target surface) and the dark-mode parity visual check.
- `emil-design-eng`, `animation-vocabulary`, `apple-design` were consulted but produced no additional findings beyond what `review-animations`/`improve-animations` already covered for this pass — the liquid-glass engine's existing motion (scale 0.97→1 pop, not scale(0); strong custom easing curves) already matched their guidance.
